
<%= simple_form_for(Food.new, method: 'get') do |f| %>
  <%= f.input :name  %>

  <%= f.submit :SEARCH, class: 'btn btn-primary' %>

<% end %>

  <!-- MAKE A SLIDER FOR FRESHNESS -->
  <!-- Change the "name" search to "food" search -->
